/**
 * Very simple in memory data store. Loads files and makes them accessible as
 * JS objects.
 */

require('../util.js');

util.provide('cau.framework.Store');

/**
 * This object should load file data, and expose getters.
 */
cau.framework.Store = function() {
  // Configuration goes here.
};